---
import ContactForm from "@/components/forms/ContactForm.astro";
import { getLocaleFromUrl } from "@/js/localeUtils";
import { getLocalizedRoute, getTranslatedData, useTranslations } from "@/js/translationUtils";
const currLocale = getLocaleFromUrl(Astro.url);
const t = useTranslations(currLocale);
---

<section id="contact" class="site-container my-20 scroll-mt-20 md:my-32">
  <div class="mt-6 grid gap-10 lg:grid-cols-2">
    <div class="">
      <h2 class="h2 mb-4">Let's Work Together</h2>
      <p class="description mb-8">
        Open to full-time roles, contract engagements, and consulting work. If you have a data challenge that needs solving, reach out and let's talk.
      </p>
      <div class="mb-8">
        <h3 class="font-heading mb-4 text-xl leading-normal font-semibold">Reach Me Directly</h3>
        <p class="text-muted-foreground mb-2">
          <span class="font-semibold">Email:</span>
          <a
            class="text-foreground hover:text-primary-accent hover:decoration-primary-accent underline"
            href="mailto:chrisrotunno@icloud.com"
            >chrisrotunno@icloud.com</a
          >
        </p>
      </div>
      <div class="mb-8">
        <h3 class="font-heading mb-4 text-xl leading-normal font-semibold">Location & Availability</h3>
        <p class="text-muted-foreground">
          Based in St. Augustine, FL. Available for remote-first roles nationwide and hybrid arrangements in Northeast Florida. Typically respond within one business day.
        </p>
      </div>
    </div>
    <div class="">
      <div class="rounded-lg border p-8 shadow-md">
        <h3 class="font-heading tracking-px-n mb-6 text-3xl leading-tight font-semibold">
          Send Us a Message
        </h3>
        <ContactForm />
      </div>
    </div>
  </div>
</section>
