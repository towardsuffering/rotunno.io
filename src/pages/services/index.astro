---
export const prerender = true;
import AboutNumbers from "@/components/about/AboutNumbers.astro";
import Contact from "@/components/contact/Contact.astro";
import ServicesCards from "@/components/services/ServicesCards.astro";
import ServicesIntro from "@/components/services/ServicesIntro.astro";
import { getLocaleFromUrl } from "@/js/localeUtils";
import { getTranslatedData } from "@/js/translationUtils";
import BaseLayout from "@/layouts/BaseLayout.astro";

const currLocale = getLocaleFromUrl(Astro.url);
const siteData = getTranslatedData("siteData", currLocale);
---

<BaseLayout
  title="What We Offer — WaterLimon Dewds"
  description="The Ancient City Splash — watermelon, lime, ice, and a dash of salt. Six variations, all made fresh on-site at events across Florida."
>
  <ServicesIntro class="mt-8" />
  <ServicesCards />
  <AboutNumbers />
  <Contact />
</BaseLayout>
